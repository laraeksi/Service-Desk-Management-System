services:
  - type: web
    name: service-desk-management-system
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node --loader tsx src/server.ts
    envVars:
      - key: NODE_ENV
        value: production
